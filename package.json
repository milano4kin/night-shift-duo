{
  "name": "night-shift-duo",
  "version": "8.0.0",
  "private": true,
  "description": "Night Shift Duo v8.0 account, onboarding, lobby and PostgreSQL persistence.",
  "main": "db_bridge.js",
  "scripts": {
    "start": "node auth_policy_patch.js && node hud_layout_patch.js && node network_smoothing_patch.js && node copy_polish_patch.js && node lobby_tools_patch.js && node lobby_visibility_patch.js && node db_bridge.js",
    "start:legacy": "node server.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "pg": "8.16.3",
    "ws": "8.21.0"
  },
  "engines": {
    "node": ">=20 <23"
  }
}
